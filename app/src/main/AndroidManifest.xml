<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="listalouvores.shello.listalouvores">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".salvacao" />
        <activity android:name=".Louvor" />
        <activity android:name=".VoltaJesus" />
        <activity android:name=".ceia" />
        <activity android:name=".Dedicacao" />
        <activity android:name=".encorajamento" />
        <activity android:name=".avivamento" />
        <activity android:name=".oracao" />
        <activity android:name=".criancas" />
        <activity android:name=".novos" />
        <activity android:name=".ensaiar"></activity>
    </application>

</manifest>